#!/bin/bash

PROJECT_ROOT=$(dirname $(dirname $(realpath $0)))

$PROJECT_ROOT/bin/assert_mflow_is_setup || exit 1

export MFLOW_LOGGING_CONFIG=test

$PROJECT_ROOT/venv/bin/python3 $PROJECT_ROOT/src/run_tests.py $@
