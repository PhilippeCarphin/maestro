#!/bin/bash

PROJECT_ROOT=$(dirname $(dirname $(realpath $0)))

$PROJECT_ROOT/bin/assert_python3_is_setup || exit 1

export MAESTRO_PYTHON3_LOGGING_CONFIG=find_node_paths

$PROJECT_ROOT/venv/bin/python3 $PROJECT_ROOT/src/maestro_search_cli.py $@ 
