<NODE_RESOURCES>
    <BATCH machine="${BACKEND}" queue="${BACKEND_DEFAULT_Q}" memory="${NEMO_MEMORY}" wallclock="${NEMO_CPUTIME}" mpi="1" cpu="${NEMO_NPEX}x${NEMO_NPEY}" catchup="1"/>
</NODE_RESOURCES>
